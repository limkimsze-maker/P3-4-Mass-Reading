name: Shared Hook Version Bumper Disabled for Practice 4
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Practice 4 no longer loads the shared medal hook."
