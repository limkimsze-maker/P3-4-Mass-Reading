name: Shared Medal Hook Disabled for Practice 4
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Practice 4 is self-contained. Do not inject the shared medal hook."
